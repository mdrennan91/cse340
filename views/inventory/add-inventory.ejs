<div class="inventory-container">
    <h1>Add New Vehicle</h1>
    <p class="form-note">All fields are required</p>
    <% if (messages.length > 0) { %>
        <div class="flash-messages">
            <% messages.forEach(message => { %>
                <p><%= message %></p>
            <% }) %>
        </div>
    <% } %>
    <% if (errors) { %>
        <ul class="error-messages">
            <% errors.array().forEach(error => { %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
    <% } %>
    <form action="/inv/add-inventory" method="POST" novalidate>
        <label for="classification_id">Classification:</label>
        <%- classificationList %>
        <label for="inv_make">Make:</label>
        <input type="text" id="inv_make" name="inv_make" value="<%= inv_make || '' %>" placeholder="Honda" required>
        <label for="inv_model">Model:</label>
        <input type="text" id="inv_model" name="inv_model" value="<%= inv_model || '' %>" placeholder="Civic" required>
        <label for="inv_description">Description:</label>
        <textarea id="inv_description" name="inv_description" placeholder="Add vehicle description here." required><%= inv_description || '' %></textarea>
        <label for="inv_image">Image Path:</label>
        <input type="text" id="inv_image" name="inv_image" value="<%= inv_image || '' %>" placeholder="/images/vehicles/example.png" required>
        <label for="inv_thumbnail">Thumbnail Path:</label>
        <input type="text" id="inv_thumbnail" name="inv_thumbnail" value="<%= inv_thumbnail || '' %>" placeholder="/images/vehicles/example.png" required>
        <label for="inv_price">Price:</label>
        <input type="number" id="inv_price" name="inv_price" value="<%= inv_price || '' %>" placeholder="Decimal or Integer" required>
        <label for="inv_year">Year:</label>
        <input type="number" id="inv_year" name="inv_year" value="<%= inv_year || '' %>" placeholder="2024" required>
        <label for="inv_miles">Miles:</label>
        <input type="number" id="inv_miles" name="inv_miles" value="<%= inv_miles || '' %>" placeholder="Digits only" required>
        <label for="inv_color">Color:</label>
        <input type="text" id="inv_color" name="inv_color" value="<%= inv_color || '' %>" placeholder="Red" required>
        <button type="submit">Add Vehicle</button>
    </form>
</div>